<%= render 'recipes' %>
<ul>
<% @recipes.each do |recipe| %>
    <li><%= link_to recipe.name, recipe_path(recipe) %> - Skill Level: <%= recipe.skill_level %></li>
<% end %>
</ul>
<br><br>


<% if !params[:country_id] %>
    <%= link_to "I only want to see dinner recipes", dinner_path %>
<% end %>